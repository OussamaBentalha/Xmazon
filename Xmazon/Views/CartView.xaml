<?xml version="1.0" encoding="UTF-8"?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms" xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" x:Class="Xmazon.CartView" Title="Panier" BackgroundColor="#428bca">
	<ContentPage.ToolbarItems>
		<ToolbarItem Text="Order" Clicked="OnOrderButtonClicked" />
		<ToolbarItem Text="Logout" Clicked="OnLogoutButtonClicked" />
	</ContentPage.ToolbarItems>
	<ContentPage.Content>
		<ListView  x:Name="lstView" BackgroundColor="#428bca">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <ViewCell>
                        <StackLayout BackgroundColor="#428bca"
                        Orientation="Vertical">
                            <StackLayout Orientation="Horizontal" Padding="8">
                             <Label Text="{Binding quantity}"
                                TextColor="#fff" Font="Bold,20"/>
                                <Label Text="{Binding product.name}"
                                TextColor="#fff" Font="Bold,20"/>
                                <Label Text="{Binding product.price, StringFormat=\{0\} €}"
                                HorizontalOptions="EndAndExpand"
                                TextColor="#fff" Font="Bold,20"/>
                            </StackLayout>
                        </StackLayout>
                    </ViewCell>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
	</ContentPage.Content>
</ContentPage>